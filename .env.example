# Folio Docker Configuration
# Copy this file to .env and update the values

# REQUIRED: Path to your Calibre library on the host machine
# This should be the directory containing metadata.db
CALIBRE_LIBRARY=/path/to/your/Calibre Library

# OPTIONAL: Hardcover API token for book discovery features
# Get your token from: https://hardcover.app/account/api
HARDCOVER_TOKEN=

# OPTIONAL: Prowlarr integration for book searching
# PROWLARR_URL=http://prowlarr:9696
# PROWLARR_API_KEY=your-api-key

# OPTIONAL: Import folder for automatic book imports
# Books placed in this folder will be automatically added to your Calibre library
# IMPORT_FOLDER=/path/to/your/Downloads/Books

# Import folder settings (only used if IMPORT_FOLDER is set)
# IMPORT_INTERVAL=60          # How often to scan for new files (seconds, default: 60)
# IMPORT_RECURSIVE=true       # Scan subdirectories for ebooks (default: true)
# IMPORT_DELETE=false         # Delete original files after import (default: false, copies instead)
